#!/usr/bin/env bash
set -euo pipefail

# Delegate the canonical Demo lifecycle without adding shell-side policy.

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd -- "$SCRIPT_DIR/.." && pwd)"

exec python3 "$PROJECT_ROOT/tools/demo_lifecycle.py" "$@"
