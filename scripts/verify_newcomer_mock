#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

exec uv run --project "$PROJECT_ROOT/hermes_temi_bridge" --extra mqtt \
  python "$PROJECT_ROOT/tools/verify_newcomer_mock.py" "$@"
